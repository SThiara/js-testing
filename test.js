/* let testString = "this is a string";

let newString = testString.split("");

newString.forEach(function(element, index, array) {
  array[index] = element.toUpperCase();
});

testString = testString.toUpperCase();
console.log(testString); */

let testArray = ["pain"];
let check = (typeof testArray);
console.log(check);